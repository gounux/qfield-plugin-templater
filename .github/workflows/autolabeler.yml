name: "🏷️ Auto label"
on:
  - pull_request_target

jobs:

  labeler:

    name: "📂 Label PR"
    runs-on: ubuntu-latest

    permissions:
      contents: read
      pull-requests: write

    steps:
      - uses: actions/labeler@v6
