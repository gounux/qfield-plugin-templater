[project]
name = "qfield-plugin-templater"
version = "0.1.0"
description = "A templater that generates a QField plugin with fancy tooling."
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "zensical>=0.0.46",
]
